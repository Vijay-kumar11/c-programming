#include<stdio.h>
int main()
{
int i,number;
	printf("enter the number");
	scanf("%d",&number);
	for(i=0;i<=number;i++)
	{
		if(number==i*i)
		printf("%d is a perfect square\n",number);
		else
		printf("%d is not a perfect square\n",number);
		return 0;
	}
}
