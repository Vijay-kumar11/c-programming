#include<stdio.h>
int main()
{
float pie=3.14;
	int radius;
printf("enter the radius of circle");
scanf("%d",&radius);
printf("the radius of circle  is %d\n",radius);
float area=(float) (pie*radius*radius);
printf("the area of the crcle is%f",area);
return 0;	
